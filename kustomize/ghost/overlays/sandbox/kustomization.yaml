apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: sandbox

generatorOptions:
  disableNameSuffixHash: true

secretGenerator:
  - name: ghost-smtp
    envs:
      - smtp.env

resources:
  - ../../base
  - ingress.yaml

patches:
  - path: patch-deployment.yaml
  - path: patch-pvc.yaml
